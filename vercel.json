{
  "buildCommand": "npm --prefix mini-app install && npm --prefix mini-app run build",
  "outputDirectory": "mini-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
